{
  "name": "csc",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "run": "tsc && func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/cosmos": "^3.14.1",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^3.3.3"
  }
}
